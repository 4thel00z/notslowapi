[project]
name = "notslowapi-site"
version = "0.0.0"
description = "Builds notslowapi.com: landing page, benchmarks page and docs"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.5",
]

[tool.uv]
package = false
